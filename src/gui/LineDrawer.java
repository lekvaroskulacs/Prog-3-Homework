package gui;

import program.DrawnLine;
import program.Table;

public class LineDrawer {
	
	static public void drawLine(Table t) {
		DrawnLine line = t.getLine();
		if (line.numOfNodes() == 1) {
			//Field line.getStart();
		}
	}
	
}
